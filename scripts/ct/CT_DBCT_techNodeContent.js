/*
  ========================================
  Section: Definition
  ========================================
*/


    const DBCT_techNodeContent = require("lovec/temp/sta/DBCT_techNodeContent");


/*
  ========================================
  Section: Application
  ========================================
*/


    /* <------------------------------ dbct0tech (mining) ------------------------------> */


    const dbct0tech_advancedMining = extendBase(
        DBCT_techNodeContent, "dbct0tech-advanced-mining",
    );


    const dbct0tech_harvester = extendBase(
        DBCT_techNodeContent, "dbct0tech-harvester",
    );


    const dbct0tech_farming = extendBase(
        DBCT_techNodeContent, "dbct0tech-farming",
    );


    /* <------------------------------ dbct0tech (item) ------------------------------> */


    const dbct0tech_massDriver = extendBase(
        DBCT_techNodeContent, "dbct0tech-mass-driver",
    );


    const dbct0tech_lootTransportation = extendBase(
        DBCT_techNodeContent, "dbct0tech-loot-transportation",
    );


    const dbct0tech_advancedLogistics = extendBase(
        DBCT_techNodeContent, "dbct0tech-advanced-logistics",
    );


    const dbct0tech_coreLogistics = extendBase(
        DBCT_techNodeContent, "dbct0tech-core-logistics",
    );


    /* <------------------------------ dbct0tech (fluid) ------------------------------> */


    const dbct0tech_pressurization = extendBase(
        DBCT_techNodeContent, "dbct0tech-pressurization",
    );


    const dbct0tech_advancedFluidTransportation = extendBase(
        DBCT_techNodeContent, "dbct0tech-advanced-fluid-transportation",
    );


    /* <------------------------------ dbct0tech (power) ------------------------------> */


    const dbct0tech_advancedPowerTransmission = extendBase(
        DBCT_techNodeContent, "dbct0tech-advanced-power-transmission",
    );


    const dbct0tech_heatTransfer = extendBase(
        DBCT_techNodeContent, "dbct0tech-heat-transfer",
    );


    const dbct0tech_rotationalEnergy = extendBase(
        DBCT_techNodeContent, "dbct0tech-rotational-energy",
    );


    /* <------------------------------ dbct0tech (factory) ------------------------------> */


    const dbct0tech_utilityCrafting = extendBase(
        DBCT_techNodeContent, "dbct0tech-utility-crafting",
    );


    const dbct0tech_oreSelection = extendBase(
        DBCT_techNodeContent, "dbct0tech-ore-selection",
    );


    const dbct0tech_heatExchange = extendBase(
        DBCT_techNodeContent, "dbct0tech-heat-exchange",
    );


    const dbct0tech_advancedFurnace = extendBase(
        DBCT_techNodeContent, "dbct0tech-advanced-furnace",
    );


    const dbct0tech_chemicalReaction = extendBase(
        DBCT_techNodeContent, "dbct0tech-chemical-reaction",
    );


    const dbct0tech_pollutionControl = extendBase(
        DBCT_techNodeContent, "dbct0tech-pollution-control",
    );


    /* <------------------------------ dbct0tech (recipe) ------------------------------> */


    const dbct0tech_alloySteelMakingI = extendBase(
        DBCT_techNodeContent, "dbct0tech-alloy-steel-making-i",
    );


    const dbct0tech_latexProcessing = extendBase(
        DBCT_techNodeContent, "dbct0tech-latex-processing",
    );


    const dbct0tech_semicokeProcessing = extendBase(
        DBCT_techNodeContent, "dbct0tech-semicoke-processing",
    );


    const dbct0tech_glassware = extendBase(
        DBCT_techNodeContent, "dbct0tech-glassware",
    );
