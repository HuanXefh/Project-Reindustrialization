/*
  ========================================
  Section: Definition
  ========================================
*/


  /* <---------- import ----------> */


  const TEMPLATE = require("lovec/temp/blk/BLK_materialBlock");


/*
  ========================================
  Section: Application
  ========================================
*/


  /* <---------- pay0mat ----------> */


  const pay0mat_woodenBlock = extendBlock(
    TEMPLATE, "pay0mat-wooden-block",
  );


  const pay0mat_copperBlock = extendBlock(
    TEMPLATE, "pay0mat-copper-block",
  );


  const pay0mat_leadBlock = extendBlock(
    TEMPLATE, "pay0mat-lead-block",
  );


  const pay0mat_castIronBlock = extendBlock(
    TEMPLATE, "pay0mat-cast-iron-block",
  );


  const pay0mat_steelBlock = extendBlock(
    TEMPLATE, "pay0mat-steel-block",
  );


  const pay0mat_rubberBlock = extendBlock(
    TEMPLATE, "pay0mat-rubber-block",
  );


  const pay0mat_brickClay = extendBlock(
    TEMPLATE, "pay0mat-brick-clay",
  );


  const pay0mat_brickMagnesia = extendBlock(
    TEMPLATE, "pay0mat-brick-magnesia",
  );


  const pay0mat_brickHighAlumina = extendBlock(
    TEMPLATE, "pay0mat-brick-high-alumina",
  );


  const pay0mat_brickMullite = extendBlock(
    TEMPLATE, "pay0mat-brick-mullite",
  );


  const pay0mat_brickSilica = extendBlock(
    TEMPLATE, "pay0mat-brick-silica",
  );


  const pay0mat_brickCarbon = extendBlock(
    TEMPLATE, "pay0mat-brick-carbon",
  );
